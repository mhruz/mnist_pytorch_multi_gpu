# mnist_pytorch_multi_gpu
This repo is used to run training and testing of MNIST handwritten digits dataset using PyTorch on multiple GPUs.

Can be executed without parameters:
 
```
python main.py
```

To get hepl:

```
python main.py -h
```